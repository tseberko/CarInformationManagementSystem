spring:
  jpa:
    show-sql: true
    open-in-view: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: titicaca
    url: jdbc:mysql://localhost:3306/car_information
server:
  error:
    include-message: always